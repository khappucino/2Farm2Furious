import Quick
import Nimble
import RxSwift


@testable import _Farm2Furious

class LoginViewSpec: QuickSpec {
    override func spec() {
        
        describe("LoginView") {
            var subject: LoginView!
            beforeEach {
                subject = LoginView(frame: CGRect(x: 0, y: 0, width: 300, height: 300))
            }
        }
        
        
        
    }
}
